_include_yml: orthrus_non_snooped
training:
  temporal_contrastive:
    enabled: True
    loss_weight: 0.1
    temperature: 0.2
    projection_dim: 64
    warmup_epochs: 2
    max_pairs_per_batch: 2048
